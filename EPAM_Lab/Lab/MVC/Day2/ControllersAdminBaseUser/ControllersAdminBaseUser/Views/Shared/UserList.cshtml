@model IEnumerable<ControllersAdminBaseUser.Models.UserViewModel>
@{
    ViewBag.Title = "User List";
}
<h2>User List</h2>

@if (!Model.Any())
{
    <h3>List of users is empty</h3>
}
@foreach (var user in Model)
{
    <p>@user.FirstName</p>
}